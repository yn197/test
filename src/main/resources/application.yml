jdbc:
  driverClassName: com.mysql.jdbc.Driver
  url: jdbc:mysql://127.0.0.1:3306/yn
  username: root
  password: root
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test3
    username: root
    password: root
server:
  port: 80
logging:
  level:
    com.itheima: debug
    org.springframework: info
# mybatis配置
mybatis:
# 实体类别名包路径
  type-aliases-package: com.itheima.pojo
  # 映射文件路径 #mapper-locations: classpath:mappers/*.xml
  configuration: # 控制台输出执行sql
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl